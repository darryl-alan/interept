<h1 class='text-center'><%= @user.username %>'s page</h1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 text-center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h4 class='text-center'><%= @user.username %>'s reports</h4>
<div class='text-center'><%= will_paginate @user_reports %></div>
<%= render 'reports/reports', reports: @user_reports %>
<div class='text-center'><%= will_paginate @user_reports %></div>
